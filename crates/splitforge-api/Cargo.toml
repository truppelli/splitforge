[package]
name = "splitforge-api"
description = "Local API served on a Unix socket. Never binds a port (ADR-0021)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
axum.workspace = true
serde.workspace = true
# The shape of the answer uses the project's vocabulary for a clock state rather than a
# second spelling of it. Types only — this crate still opens nothing.
splitforge-domain.workspace = true
thiserror.workspace = true
tokio.workspace = true

[lints]
workspace = true

[dev-dependencies]
serde_json.workspace = true
tempfile.workspace = true
tokio = { workspace = true, features = ["io-util", "rt-multi-thread"] }
